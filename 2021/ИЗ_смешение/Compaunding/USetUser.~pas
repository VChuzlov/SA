unit USetUser;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls;

type
  TForm21 = class(TForm)
    BtOk: TButton;
    BtCan: TButton;
    Label1: TLabel;
    EdRash: TEdit;
    procedure BtOkClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form21: TForm21;

implementation

uses UPlan, Grids;

{$R *.dfm}

procedure TForm21.BtOkClick(Sender: TObject);
begin
  with FPlan.SGUser do
  begin
    //:=StrToFloat(Cells[Selection.Top, Selection.Left]);
  end;
end;

end.
